import { Typography} from "@material-ui/core"
import * as React from "react"

/**
 * Page to be shown when what the user is looking for can't be found.
 */
export const NotFoundPage: React.StatelessComponent = props => (
	<Typography variant="title">404 - Not Found</Typography>
);
